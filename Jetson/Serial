import serial
import time

class get_serial:
    def get_serial:
    def add_tty:

uno = serial.Serial('dev/ttyACM0', 115200, timeout=5)
mega = serial.Serial('dev/ttyACM1', 115200, timeout=5)

uno.write();
uno.read();